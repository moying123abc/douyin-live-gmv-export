# 逐字节比对用的 golden 夹具按"非文本"处理,禁止 Git 做换行符转换,
# 否则在不同 core.autocrlf 设置或跨平台克隆时字节比对会失效。
tests/fixtures/*.csv -text

# CSV 输出产物保留生成时的换行符(不参与归一化)
*.csv -text

# Windows 批处理必须 CRLF:用 LF 会让 cmd.exe 错行解析(实测会静默不执行命令)
*.bat text eol=crlf
