# 真机联调结论(2026-09-04)

本机(用户已完成人工登录,持久化 profile 有效,Playwright 1.62 就绪)对
room_id=`7000000000000000002` 的**已结束场次回放页**完成了 probe 证据采集与在线
export 验证。目标页面实为**抖音生活服务/本地直播专业版**直播大屏
(`eos.douyin.com/dp/liveScreen`,生活服务商户数据;账号对该场次有权限)。

## 1. 探测结果与证据文件

命令:`python main.py probe --room-id 7000000000000000002`

- 登录态自动复用(无需重新扫码);场次状态 `ended`;
- **L0 未命中**(页面图表非 ECharts 实例,instances=0);
- **L1 命中**:`rows=465,时间格式=MM-DD HH:MM`,退出码 0;
- 证据文件:`data/probe/probe_7000000000000000002_20260904_205812.json`

证据要点(field_definition/session_bounds/row_sample 已记录):
- 来源 URL:`https://eos.douyin.com/life/api/live_screen/v5/room_minute_indicator`
- 序列 key:`pay_order_gmv_minute_trend`(趋势「成交金额」分钟序列)
- 结构:行对象数组 `data[].chart[]`,每点 `{x:'09-02 11:16', y:0, time_stamp:'1788318960'}`
  —— `x` 为页面自带时间 `MM-DD HH:MM`(无年份),`y` 为数值(元);
- 整场起止:`09-02 11:16 ~ 09-02 19:00`(465 行 = 464 分钟 + 1,与场次 7h44m 对齐)。

## 2. 口径核对(依据真实页面证据)

| 项 | 页面/接口事实 | 结论 |
|----|---------------|------|
| time 粒度与格式 | `x` = `MM-DD HH:MM` 分钟点,原样保留 | 与导出口径一致(time 用页面自带时间) |
| gmv_min 语义 | `y` 单位 元,`pay_order_gmv_minute_trend` 标题「成交金额」 | 该分钟新增成交金额(元) |
| 时间轴连续 | 465 行分钟连续,含金额 0 的分钟(456 个 0 分钟,9 个非零分钟) | 校验通过 |
| 非零分钟求和 ≈ 页面累计 | 全行求和 = **1350.00 元** = `key_index.PayGmv`「直播间成交金额/累计支付金额」= 1350 元 | **相等**,求和校验通过 |

- 场次元信息:room_info `start_time/finish_time`(epoch),session_date 由行对象
  `time_stamp` 推断为 `2026-09-02`(x 无年份,故日期列来自 epoch,真机正确);
- 与夹具假设差异:夹具用“等长 时间数组+数值数组”或 rows 数组,M2 原 `_find_full_series`
  认不出“行对象列表 {x,y,time_stamp}”的真实形状 → 需要最小适配(见下)。

## 3. 在线 export 验证

命令:`python main.py export --room-id 7000000000000000002 --excel --out data\outputs\live_7000000000000000002`

- 退出码 0,打印 `校验通过 ✓`;三条内置校验全部通过
  (行数 465=464+1;时间轴严格递增且按分钟连续;非零分钟求和 1350.00 ≈ 页面累计 1350.00);
- 输出:`data/outputs/live_7000000000000000002.csv`(UTF-8 BOM,465 行,表头
  room_id/session_date/time/gmv_min)与 `.xlsx`(openpyxl);
- 附加断言:CSV 求和 1350.0、465 行、0 分钟保留(456 个)均实测通过。

## 4. 最小适配(在既有 inScope 范围内)

1. `extractor/trend.py`
   - 新增“行对象序列”识别 `_find_row_series`(列表元素 `{x, y, time_stamp}` 等),
     L1 选优规则:排除商品/营销端点(product_trend、marketing_data、follow_product、
     product_explanation、sales_tool → **范围外,不收集**),GMV 语义优先
     (`pay_order_gmv_minute_trend`),长度相同取 gmv 序列;
   - `_find_cumulative_key_index`:从 `key_index.PayGmv` 读取页面累计直播间成交金额,
     使求和校验可自动启用;
   - session_date 用行对象 `time_stamp`(epoch)推断(页面 x 无年份);
   - `read_live` docstring 更新为真机验证口径。
2. `probe.py`
   - `_scan_row_objects` 行对象扫描 + 越界 URL 排除 + `series_key` 证据;
   - `_try_l1`/证据输出:记录 `series_key`(pay_order_gmv_minute_trend)、时间格式与
     行样例(rows_sample);`_ResponseRecorder.read_bodies` 上限 12 → 64(分钟指标响应
     加载中后期才返回,原上限会被 monitor/marketing 早期响应挤掉);
   - 默认 `load_wait_seconds` 25 → 35(与 config.example.yaml/config.py 同步)。
3. 新增离线单元 `tests/test_trend_row_series.py`(行对象识别/GMV 优先/product URL
   排除/key_index 累计);真实证据文件不入 fixtures(含账号业务数据,放 gitignore 的
   data/probe)。

## 5. 回归

适配后全量回归保持全绿:py_compile 全模块 0;M1 selfcheck 0;M2 10/10;M3 9/9
(含开关关闭输出与 M2 参考 CSV 逐字节一致);cli_export_check 0;新增 T8 结构用例 2/2;
离线 `export --fixture` 校验通过。

## 6. 范围与合规确认

- 只读取本账号有权限的已结束场次数据;被动捕获浏览器自身发起的响应,无改写/伪造;
- 未做登录绕过、未做签名逆向;
- 商品(product_trend/follow_product)、营销(marketing_data/sales_tool)等时间轴
  在读取层显式排除,**未收集**;不涉及违规事件时间轴;
- profile/凭据未提交;真实证据留在 data/probe(data/browser_profile、data/probe、
  data/outputs 均不入库)。

## 7. 遗留待复核(换场次/换账号时)

- 其他场次/账号下分钟序列 key 是否仍为 `pay_order_gmv_minute_trend`,x/y 语义是否一致;
- `key_index.PayGmv`(直播间成交金额,单位 元)与 gmv 分钟求和是否仍一致;
- 直播场次跨午夜时 x(`MM-DD HH:MM`)的日期归属与时间轴连续性核对。
